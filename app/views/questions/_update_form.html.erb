
<%= form.fields_for :questions do |question_form| %>
  <tr>
  <div class="field">
    <td><%= question_form.text_field :body %></td>
    <td><%= question_form.check_box :_destroy %></td>
  </div>
  </tr>
  <tr><td>
    Current Option Count: <%= question_form.object.choices.length %>  |
    <%= link_to "Manage Options", manage_choices_path(@poll.edit_key, question_form.object.id) %>


    </td><td></td></tr>
<% end %>

